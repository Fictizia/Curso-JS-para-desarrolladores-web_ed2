/*global define*/
define([
	'jquery',
	'backbone',
], function ($, Backbone) {
	'use strict';
	var misRecetasAppRouter = Backbone.Router.extend({
		routes: {
		}
	});

	return misRecetasAppRouter;
});